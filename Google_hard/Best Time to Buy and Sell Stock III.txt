/*

You are given an array prices where prices[i] is the price of a given stock on the ith day.

Find the maximum profit you can achieve. You may complete at most two transactions.

Note: You may not engage in multiple transactions simultaneously (i.e., you must sell the stock before you buy again).

 

Example 1:

Input: prices = [3,3,5,0,0,3,1,4]
Output: 6
Explanation: Buy on day 4 (price = 0) and sell on day 6 (price = 3), profit = 3-0 = 3.
Then buy on day 7 (price = 1) and sell on day 8 (price = 4), profit = 4-1 = 3.
Example 2:

Input: prices = [1,2,3,4,5]
Output: 4
Explanation: Buy on day 1 (price = 1) and sell on day 5 (price = 5), profit = 5-1 = 4.
Note that you cannot buy on day 1, buy on day 2 and sell them later, as you are engaging multiple transactions at the same time. You must sell before buying again.
Example 3:

Input: prices = [7,6,4,3,1]
Output: 0
Explanation: In this case, no transaction is done, i.e. max profit = 0.
 

Constraints:

1 <= prices.length <= 105
0 <= prices[i] <= 105

*/


int maxProfit(vector<int>& prices) {

        // here we need to think in this way that our first profit is extra income and that can be used to leverage our second purchase.

        int cost1=INT_MAX; //this maintains the lowest possible value.
        int sell_one=0; //this maintains the maximum profit that we can get by first selling from cost1

        int cost2=INT_MAX; //this maintains our second buy with the leverage of first profit.
        int sell_two=0; // this is the final profit that we make after the second selling. or overall profit.

        for(auto ele: prices){
            cost1=min(cost1,ele);
            sell_one=max(sell_one,ele-cost1);

            cost2=min(cost2,ele-sell_one);
            sell_two=max(sell_two,ele-cost2);
        }
        return sell_two;
        
    }

