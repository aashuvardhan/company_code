/*
https://leetcode.com/problems/maximum-product-of-splitted-binary-tree/description/?envType=daily-question&envId=2026-01-07

Given the root of a binary tree, split the binary tree into two subtrees by removing one edge such that the product of the sums of the subtrees is maximized.

Return the maximum product of the sums of the two subtrees. Since the answer may be too large, return it modulo 109 + 7.

Note that you need to maximize the answer before taking the mod and not after taking it.



*/

int build_traversal(TreeNode* root){
        if(root==NULL){
            return 0;
        }
        int l=build_traversal(root->left);
        int r=build_traversal(root->right);
        root->val+=l+r;
        return root->val;
    }

    void traversal(TreeNode* root,long long& res,int& sum){
        if(root==NULL){
            return;
        }
        long long rem=sum-root->val;
        res=max(res,rem*root->val);

        traversal(root->left,res,sum);
        traversal(root->right,res,sum);

        
    }


    int maxProduct(TreeNode* root) {
        long long mod = 1e9+7;
        long long res=0;
        int sum=build_traversal(root);
        if(root->left)
            traversal(root->left,res,sum);
        if(root->right)
            traversal(root->right,res,sum);
        
        return (res%mod);
    }